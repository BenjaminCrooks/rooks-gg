<!DOCTYPE html>
<html lang="en">

<head>
<title>Rooks - <%= pageTitle %></title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="author" content="Rooks">
<meta name="description" content="Baseline page template">

<link rel="icon" href="assets/icons/rooksIconGG.png">
<link rel="stylesheet" href="styles.css">
<script src="plugins/jquery-3.4.1.min.js"></script>

<style>

	body {
		min-height: calc(100vh - 56px);
	}

	header {
		gap: 32px;
	}

	input#search-bar {
		width: 25%;
	}

	main {
		display: grid;
		gap: 24px;
  		align-items: center;
  		grid-template-columns: repeat(10, 1fr);
	}

	main > a {
		width: 100%;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 8px;
		transition: opacity 0.25s ease-out;
	}

	div.frame {
		width: 100%;
		aspect-ratio: 1 / 1;
		border-radius: 4px;
		overflow: hidden;
	}

	div.frame > img {
		width: 100%;
		object-fit: cover;
    	transition: transform 0.25s ease-out;
/*    	transform: scale(1.025);*/
	}

	main:hover > a {opacity: .5;}
	main > a:hover {opacity: 1;}
	main > a:hover img {transform: scale(1.1);}

	label {
		font-family: Roboto;
		color: #989898;
	}

</style>

</head>

<body>

<%- include("partials/navigation-bar") %>

<header class="v-container">
	<h1><%= pageTitle %></h1>
	<input type="text" autocomplete="off" id="search-bar" placeholder="Search">
</header>

<main>

	<% champions.forEach(function(champion, index) { %>
	<a href="<%= urlPath %><%= champion %>">
		<div class="frame"><img src="assets/dragontail/champion/square106/<%= champion %>.png"></div>
		<label><%= champion %></label>
	</a>
	<% }) %>

</main>


<script>$(document).ready(function() {})</script>

</body>

</html>